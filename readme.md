Setelah clone project silahkan install virtual environment dulu
    
    python -m venv venv

Jika sudah install virtual environment silahkan install beberapa library berikut

    pip install flask dnspython pymongo
    